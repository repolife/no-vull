name: no-vull security scan

on:
  push:
    paths:
      - "package-lock.json"
      - "package.json"
  pull_request:
    paths:
      - "package-lock.json"
      - "package.json"
  schedule:
    - cron: "0 8 * * 1"   # every Monday 8am UTC
  workflow_dispatch:

jobs:
  vulnerability-scan:
    name: Vulnerability scan
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install project dependencies
        run: npm ci

      - name: Build no-vull
        run: npm run build

      - name: Check GitHub status
        run: node dist/cli.js status
        continue-on-error: true

      - name: Run vulnerability scan
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node dist/cli.js . --exit-code

      - name: Run health check against standards
        run: node dist/cli.js check . --fail-on outdated

  supply-chain:
    name: Supply-chain check
    runs-on: ubuntu-latest
    # Run weekly or on lockfile changes only — slower due to registry calls
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install project dependencies
        run: npm ci

      - name: Build no-vull
        run: npm run build

      - name: Check GitHub status
        run: node dist/cli.js status
        continue-on-error: true

      - name: Supply-chain audit
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: node dist/cli.js . --supply-chain --exit-code
